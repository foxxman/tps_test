{
  "private": true,
  "workspaces": {
    "packages": [
      "services/backend"
    ],
    "nohoist": [
      "**/.prisma/**",
      "**/.prisma",
      "**/@prisma/**",
      "**/@prisma",
      "**/prisma/**",
      "**/prisma"
    ]
  },
  "scripts": {
    "start:docker": "docker-compose --compatibility -p tps_test -f deploy/local/docker-compose.yml up --force-recreate --build -d",
    "prisma:generate": "yarn workspace backend run prisma generate"
  }
}
